//TODO: escrever função para validar o token de rotas autenticadas

import type { Request, Response } from "express";

function validateAccessResource(req: Request, res: Response) {}
export default validateAccessResource;
